.b-feeds
  %h1
    Все твиттеры ВГУ ФКН

  %form{class: "well form-search", action: "add", method: "post"}
    %input{type: "text", class: "input-medium search-query", name: "username"}
    %button{type: "submit", class: "btn"}
      Добавить

  %table
    %tr
      %th= "Юзернейм"
      %th= "Количество твитов"
    - @feeds.each do |feed|
      %tr
        %td
          %a{href: "http://twitter.com/#{feed.username}"}
            = feed.username
        %td
          = feed.tweets_counter
